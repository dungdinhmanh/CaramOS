# CaramOS — Package cần THÊM vào ISO
# Mint ISO đã có: wifi, ethernet, bluetooth, linux-firmware
# Chỉ thêm những gì Mint CHƯA CÓ

# Display Manager (thay LightDM mặc định)
sddm
sddm-theme-breeze

# Driver bổ sung (tên Ubuntu/Mint)
linux-firmware
dkms
